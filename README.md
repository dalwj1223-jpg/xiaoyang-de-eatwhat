# GitHub Pages 扁平部署包

## 必须这样上传

1. 解压 ZIP。
2. 打开解压后的文件夹，全选里面的所有文件。
3. 上传到 GitHub 仓库根目录，确保 `index.html` 与所有 `.svg`、`.jpg`、`.png` 文件处于同一级。
4. 不要把 ZIP 文件本身上传到仓库，也不要只上传 HTML、CSS、JS。
5. 在 GitHub Pages 中选择该分支的 `/ (root)` 目录。

本版本不使用 assets 子目录，避免 GitHub 网页上传时遗漏文件夹。